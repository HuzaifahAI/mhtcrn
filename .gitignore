# Datasets (downloaded locally, never pushed)
LibriSpeech/
DNS-Challenge/
*.flac
*.wav

# Python bytecode
__pycache__/
*.pyc

# Training outputs: ignore checkpoints / tensorboard / logs, keep only the small summaries
runs/**
!runs/
!runs/*/
!runs/*/test_summary.json
!runs/*/gate_stats.csv
!runs/*/config.json
!runs/*/test_metrics.csv
!runs/summary.csv
!runs/summary_by_config.csv
!runs/run_all.log!runs/*/log.txt
